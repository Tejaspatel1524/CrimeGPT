# CrimeGPT Backend

AI-assisted cybercrime investigation platform API.

## Setup

```bash
pip install -r requirements.txt
```

## Run

```bash
cd backend
uvicorn app.main:app --reload
```

## Endpoints

- `GET /health` — Health check
- `GET /docs` — Swagger UI
continue continue b